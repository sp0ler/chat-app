jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run script file
      run: |-
         chmod +x ./github/workflow/workflow.sh
         ./github/workflow/workflow.sh
      shell: bash
